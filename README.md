# Dialog Schema

To run Simulated Dialogs:
1. `cd src/simulated_dialogs`
2. `pip install -r requirements.txt`
3. Set `OPENAI_API_KEY` and `CUDA_VISIBLE_DEVICES` in environment
4. `python three_bot_setup.py`